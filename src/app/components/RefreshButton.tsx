"use client";

const RefreshButton = () => {
  return (
    <button  className="text-lg bg-blue-500 text-white p-4 rounded-lg">
      Click to discover a new breed!
    </button>
  );
};

export default RefreshButton;
